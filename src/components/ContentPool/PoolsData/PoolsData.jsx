import React from 'react';
import { formatBalance } from '@polkadot/util';

export default function PoolData(props) {
	const { poolsData } = props;

	console.log(poolsData);
	return <div>Hello!</div>;
}
